<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="x-apple-disable-message-reformatting" />
  <title>Activate your <%= typeof appName !=='undefined' ? appName : 'Kepi.ai' %> account</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #f6f8fb;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      color: #0f172a;
    }

    .preheader {
      display: none;
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      color: transparent;
    }

    .wrap {
      width: 100%;
      padding: 24px 12px;
      box-sizing: border-box;
    }

    .card {
      max-width: 560px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 2px 10px rgba(2, 6, 23, 0.06);
      overflow: hidden;
    }

    .brand {
      padding: 20px 24px;
      background: #0ea5e9;
      color: #fff;
      font-weight: 700;
      font-size: 18px;
      letter-spacing: .2px;
    }

    .content {
      padding: 24px;
    }

    h1 {
      font-size: 20px;
      margin: 0 0 12px;
    }

    p {
      margin: 0 0 12px;
      line-height: 1.55;
    }

    .btn {
      display: inline-block;
      background: #0ea5e9;
      color: #fff !important;
      text-decoration: none;
      padding: 12px 20px;
      border-radius: 10px;
      font-weight: 600;
    }

    .code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      background: #f1f5f9;
      padding: 10px 14px;
      border-radius: 8px;
      display: inline-block;
      letter-spacing: 2px;
      font-size: 16px;
    }

    .muted {
      color: #64748b;
      font-size: 12px;
    }

    .divider {
      height: 1px;
      background: #e2e8f0;
      margin: 20px 0;
    }

    .footer {
      padding: 16px 24px;
      text-align: center;
      color: #64748b;
      font-size: 12px;
    }

    @media (prefers-color-scheme: dark) {
      body {
        background: #0b1220;
        color: #e2e8f0;
      }

      .card {
        background: #0f172a;
        box-shadow: none;
      }

      .brand {
        background: #0284c7;
      }

      .code {
        background: #111827;
        color: #e5e7eb;
      }

      .muted,
      .footer {
        color: #94a3b8;
      }

      .divider {
        background: #1f2937;
      }
    }
  </style>
</head>

<body>
  <div class="preheader">Activate your <%= typeof appName !=='undefined' ? appName : 'Kepi.ai' %> account</div>

  <div class="wrap">
    <div class="card">
      <div class="brand">
        <%= typeof appName !=='undefined' ? appName : 'Kepi.ai' %>
      </div>
      <div class="content">
        <h1>Welcome<% if (typeof name !=='undefined' && name) { %>, <%= name %>
              <% } %>!</h1>
        <p>Thanks for signing up for <strong>
            <%= typeof appName !=='undefined' ? appName : 'Kepi.ai' %>
          </strong>. To complete your registration, please verify your email.</p>

        <% if (typeof otp !=='undefined' && otp) { %>
          <p>Use this one-time code:</p>
          <p class="code">
            <%= otp %>
          </p>
          <p class="muted">This code expires in <%= typeof otpTtlMins !=='undefined' ? otpTtlMins : 5 %> minutes.</p>
          <div class="divider"></div>
          <% } %>

            <% if (typeof activationLink !=='undefined' && activationLink) { %>
              <p>You can also activate your account using the button below:</p>
              <p style="margin:16px 0;">
                <a class="btn" href="<%= activationLink %>" target="_blank" rel="noopener">Activate Account</a>
              </p>
              <p class="muted">If the button doesn’t work, copy and paste this URL into your browser:</p>
              <p class="muted" style="word-break:break-all;">
                <%= activationLink %>
              </p>
              <div class="divider"></div>
              <% } %>

                <p>If you didn’t create this account, you can safely ignore this email.</p>
      </div>
      <div class="footer">
        © <%= new Date().getFullYear() %>
          <%= typeof appName !=='undefined' ? appName : 'Kepi.ai' %>. All rights reserved.<br />
            Need help? Contact us at <a
              href="mailto:<%= typeof supportEmail !== 'undefined' ? supportEmail : 'support@kepi.ai' %>">
              <%= typeof supportEmail !=='undefined' ? supportEmail : 'support@kepi.ai' %>
            </a>
      </div>
    </div>
  </div>
</body>

</html>